import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = { title: "Reports" };

export default function Page() {
  return (
    <PlaceholderPage
      title="Reports"
      description="Excel and PDF exports: schedules, approval certificates, venue packs, registers and cost reports."
      phase="Phase 1"
    />
  );
}
