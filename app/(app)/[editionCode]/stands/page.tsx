import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = { title: "Stand approvals" };

export default function Page() {
  return (
    <PlaceholderPage
      title="Stand approvals"
      description="Space-only stand submissions checked for completeness, venue rules and structural complexity."
      phase="Phase 2"
    />
  );
}
