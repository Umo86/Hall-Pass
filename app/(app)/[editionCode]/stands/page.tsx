import { PlaceholderPage } from "@/components/placeholder-page";

export default function Page() {
  return (
    <PlaceholderPage
      title="Stand approvals"
      description="Space-only stand submissions — being built now."
      phase="this build"
    />
  );
}
