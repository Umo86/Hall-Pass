import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = { title: "Editions" };

export default function Page() {
  return (
    <PlaceholderPage
      title="Editions"
      description="List, create and clone event editions. Cloning copies halls, locations, deadline offsets and signage items as drafts."
      phase="Phase 1"
    />
  );
}
