import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = { title: "My Sign-offs" };

export default function Page() {
  return (
    <PlaceholderPage
      title="My Sign-offs"
      description="Pending approval steps assigned to you across all editions, sorted by due date, with inline decisions."
      phase="Phase 1"
    />
  );
}
