import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = { title: "Settings" };

export default function Page() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Users and roles, external grants, item types, workflows, venues and rules, suppliers, deadlines, email templates and organisation settings."
      phase="Phase 1"
    />
  );
}
